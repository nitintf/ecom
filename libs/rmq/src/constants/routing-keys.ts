export enum RoutingKey {
  // Auth routing keys
  AUTH_VALIDATION = 'auth.validation',

  // Product routing keys
  PRODUCT_CREATED = 'product.created',
  PRODUCT_QUANTITY_VALIDATION = 'product.quantity.validation',
}
